# Pinterest pins are per-account manual (Pinterest API needs app approval).
# Publish path: paste pin-batch.json entries via the Pinterest app/web (batch tool),
# or wire the official API once an app token exists (owner-env).
# 5 pins x per-language boards — Pinterest is a search engine: boards ARE keywords.
